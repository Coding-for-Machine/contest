pip install weasyprint qrcode

Tizim muammosiz, xatosiz va tezkor ishlashi uchun serveringizga hamda Python muhitiga (virtualenv) bir 
nechta kutubxonalarni oʻrnatishingiz kerak.WeasyPrint kutubxonasi shunchaki Python paketi emas, u tizim 
darajasidagi grafik dasturlarga bogʻliq. Shuning uchun quyidagi ikki bosqichni ketma-ket bajaring:1-Bosqich: 
Operatsion tizim paketlarini oʻrnatishTerminalni oching va operatsion tizimingizga qarab quyidagi komandani 
bosing:Linux (Ubuntu/Debian) uchun:bashsudo 
```
apt update
sudo apt install libpango-1.0-0 libharfbuzz0b libpangoft2-1.0-0 libffi-dev libjpeg-dev libopenjp2-7-dev
```

Koddan ehtiyotkorlik bilan foydalaning.macOS (Homebrew orqali) uchun:
```
brew install pango cairo gdk-pixbuf libffi
```

find . -path "*/migrations/*.py" -not -name "__init__.py" -delete
find . -path "*/migrations/*.pyc" -delete


find . -type d -name "__pycache__" -exec rm -r {} +
